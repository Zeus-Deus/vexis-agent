# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
build/

# Node / frontend
node_modules/
.vite/
*.tsbuildinfo

# Secrets
.env

.codemux/

SOUL.md

# Personal planning notes
.plans/

# Codemux dev tooling — local MCP config, not a vexis runtime artifact.
.mcp.json

# Per-brain instruction-file symlink. CLAUDE.md is canonical (gitted);
# AGENTS.md is the symlink the install script creates so opencode and
# any future AGENTS.md-reading brain can find the same content. The
# install script refuses to overwrite a real (non-symlink) AGENTS.md.
AGENTS.md
