# Python
__pycache__/
*.pyc
.venv/
venv-python
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
build/

# Node / frontend
node_modules/
.vite/
*.tsbuildinfo

# Secrets
.env

.codemux/

SOUL.md

# Personal planning notes
.plans/

# Codemux dev tooling — local MCP config, not a vexis runtime artifact.
.mcp.json

# Per-user agent context (skills, memories, settings). Not part of the
# package; ignored so private notes can't leak into the public repo.
.claude/
